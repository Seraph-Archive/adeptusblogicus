{{#each posts as |post|}}

<div class="row">

    <div class="container">
			<div class="row  justify-content-center headerRow">
      <div class="col-8">
        <h2>
          <a class="text-uppercase" href="/post/{{post.id}}">{{post.title}}</a>
        </h2>
      </div>

      <div class="col-4">
        <p class="text-right">Created by
          {{post.user.username}}
          on
          {{format_date post.date_created}}
        </p>
      </div>
			</div>
    <div>
      <p class="text-center">
        {{post.description}}
      </p>
    </div>
    </div>

  </div>

{{/each}}